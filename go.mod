module github.com/mailtea-app/mailtea-go-example

go 1.18

// The official Mailtea Go SDK. Go resolves this straight from the repository's
// v0.1.0 tag through the module proxy — for Go there is no separate registry
// form to switch to later.
require github.com/mailtea-app/mailtea-go v0.1.0
